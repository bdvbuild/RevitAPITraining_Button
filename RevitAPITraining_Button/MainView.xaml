<Window x:Class="RevitAPITraining_Button.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPITraining_Button"
             mc:Ignorable="d" 
             Height="150" Width="200"
             Title="Задание 5.1">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    <StackPanel>
        <Button Content="Количество труб" Command="{Binding PipesCount}"/>
        <Button Content="Объем стен" Command="{Binding WallsVolume}"/>
        <Button Content="Количество дверей" Command="{Binding DoorsCount}"/>
    </StackPanel>
</Window>
